services:
  warriorai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: warriorai
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: 3001
      NODE_ENV: production
    ports:
      - "3001:3001"
